{
  "name": "ndbrain",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "desktop:build": "pnpm -F @ndbrain/web build && pnpm -F @ndbrain/desktop tauri build"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
