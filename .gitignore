node_modules/
dist/
coverage/
*.log

# runtime data — never commit a vault or an index
data/
vaults/
*.db
*.db-shm
*.db-wal

.env
.env.*
!.env.example

.DS_Store
Thumbs.db

# Per-instance deployment tweaks; never the same on two machines.
docker-compose.override.yml
