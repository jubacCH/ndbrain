{
  "name": "@ndbrain/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "dev": "node --experimental-strip-types src/main.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "simple-git": "^3.36.0"
  }
}
